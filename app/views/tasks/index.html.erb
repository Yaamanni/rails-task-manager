<h1> All Tasks </h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= form_for(task) do |f| %>
<%= f.check_box :completed %>
<% end %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to "Delete", task_path(task) %></li>
  <% end %>
</ul>


 <%= link_to "Create New Task", tasks_new_path %>
